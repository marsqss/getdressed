# Get Dress App

Search for a string in a given infinite streams of data using actors.

## Software Requirements

* Java 8
* sbt 0.13 (included)

## Build


`> sh getDressBuild`

This runs the included version of `sbt`, getting dependencies, compiling, testing and packaging the app.

## Usage

`> sh getdressed` start the process using the following parameters:

### Parameters

`> sh getdressed HOT 8 6 4 2 1 7`

## Implementation

The app is developed using Scala 2.12
